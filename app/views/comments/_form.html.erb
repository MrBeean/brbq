<%= simple_form_for [@event, comment] do |f| %>
    <%= f.input :body %>
    <% unless user_signed_in? %>
      <%= f.input :user_name %>
    <% end %>
    <%= f.button :submit, 'Оставить комментарий' %>
<% end %>
