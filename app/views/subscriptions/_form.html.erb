<%= simple_form_for [@event, subscription] do |f| %>
    <% unless user_signed_in? %>
      <%= f.input :user_name %>
      <%= f.input :user_email %>
    <% end %>
    <%= f.button :submit, 'Подписаться!' %>
<% end %>
